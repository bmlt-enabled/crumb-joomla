<?php

declare(strict_types=1);

// PHPUnit bootstrap. Source files start with `\defined('_JEXEC') or die;`
// (Joomla's request-guard pattern), so define _JEXEC to keep them loadable
// outside the CMS for unit tests.
\defined('_JEXEC') or define('_JEXEC', 1);

require __DIR__ . '/../vendor/autoload.php';
